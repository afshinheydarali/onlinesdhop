services:
  bot:
    build: .
    restart: unless-stopped
    env_file: ${ENV_FILE:-.env}
    environment:
      DATABASE_PATH: /data/orders.sqlite3
    volumes:
      - orders-data:/data

volumes:
  orders-data:
